[project]
name = "ml-r-example"
version = "0.1.0"
description = "R model integration example for chapkit"
requires-python = ">=3.13"
dependencies = ["chapkit @ file:///${PROJECT_ROOT}/../.."]

[tool.uv]
dev-dependencies = []
