{% raw %}apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chapkit.fullname" . }}
  labels:
    {{- include "chapkit.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}{% endraw %}
