[project]
name = "{{ PROJECT_SLUG }}"
version = "0.1.0"
description = "{{ PROJECT_DESCRIPTION }}"
requires-python = ">=3.13"
dependencies = [
    "chapkit",
    "pandas>=2.3.3",
]

[dependency-groups]
dev = [
    "uvicorn[standard]>=0.30.0",
]

[tool.uv.sources]
chapkit = { git = "https://github.com/dhis2-chap/chapkit" }
