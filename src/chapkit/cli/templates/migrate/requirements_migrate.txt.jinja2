# Generated by `chapkit mlproject migrate` from the original pyproject.toml and/or
# pyenv.yaml / conda.yaml / MLproject env hints. The Dockerfile installs this
# file via `uv pip install -r requirements.txt`, sidestepping shell-quoting
# pitfalls with PEP 508 markers that contain nested quotes (e.g.
# `importlib-metadata; python_version < "3.10"`). Mirrors the deps listed in
# pyproject.toml; edit both in lockstep, or regenerate via `chapkit mlproject migrate`.
{% if USER_INDEX_OPTIONS %}
# Index / install directives preserved from the original requirements.txt
# (private PyPI mirrors, --find-links trees, etc.). uv and pip both honour
# these when reading the file.
{% for option in USER_INDEX_OPTIONS %}
{{ option }}
{% endfor %}

{% endif %}
{% for dep in USER_DEPENDENCIES %}
{{ dep }}
{% endfor %}
