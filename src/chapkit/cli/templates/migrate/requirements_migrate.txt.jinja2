# Generated by `chapkit mlproject migrate` from the original pyproject.toml and/or
# pyenv.yaml / conda.yaml / MLproject env hints. The Dockerfile installs deps via
# `uv sync` against the generated pyproject.toml + uv.lock; this requirements.txt
# is kept for reference and as a fallback for tooling that prefers the pip format
# (private indexes, `--trusted-host`, etc. that don't translate to pyproject.toml
# `[tool.uv]` are still preserved here).
{% if USER_INDEX_OPTIONS %}
# Index / install directives preserved from the original requirements.txt
# (private PyPI mirrors, --find-links trees, etc.). uv and pip both honour
# these when reading the file.
{% for option in USER_INDEX_OPTIONS %}
{{ option }}
{% endfor %}

{% endif %}
{% for dep in USER_DEPENDENCIES %}
{{ dep }}
{% endfor %}
